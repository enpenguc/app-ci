language: node_js
node_js:
  - "12"
# before_install:
#   - curl -o- -L https://yarnpkg.com/install.sh | bash -s
#   - export PATH="$HOME/.yarn/bin:$PATH"
# cache:
#   yarn: true
install:
  - node --version
  - npm --version
  - echo "npm install ..."
  - npm install
  - echo "npm install success."
before_script:
  - ''
script:
  - npm run test:unit

after_script:
  - echo "ci completed."
